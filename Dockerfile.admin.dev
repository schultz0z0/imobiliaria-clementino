FROM node:22-bookworm-slim

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci \
  && npm install --no-save @rollup/rollup-linux-x64-gnu@4.62.5

EXPOSE 4175

CMD ["npm", "run", "admin:dev", "--", "--host", "0.0.0.0", "--port", "4175"]
